apiVersion: v1
kind: Service
metadata:
  name: mcp-k8s-networking
  namespace: mcp-k8s-networking
  labels:
    app.kubernetes.io/name: mcp-k8s-networking
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-k8s-networking
  ports:
    - name: mcp
      port: 8080
      targetPort: mcp
      protocol: TCP
